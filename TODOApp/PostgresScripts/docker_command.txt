docker run --name tododb-postgres \
  -e POSTGRES_USER={user}} \
  -e POSTGRES_PASSWORD={password} \
  -e POSTGRES_DB=tododb \
  -p 5432:5432 \
  -v pgdata:/var/lib/postgresql/data \
  -d postgres:latest
